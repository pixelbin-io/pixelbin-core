export function getUrlParts(pixelbinUrl: any): {
    protocol: string;
    host: string;
    search: string;
    version: string;
};
import { version2Regex } from "./regex";
export { version2Regex };
